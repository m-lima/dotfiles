{
  core = {
    agenix.enable = true;
    disko.enable = true;
    home.enable = true;
    impermanence = {
      enable = true;
      wipe.enable = true;
    };
    nixos.enable = true;
    user = {
      enable = true;
    };
  };
  services = {
    ssh.enable = true;
  };
}
